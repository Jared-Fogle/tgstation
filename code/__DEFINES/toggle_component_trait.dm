// Defines to make toggle_component_trait consumers easier to read.

/// When this trait is added, add the component.
#define TOGGLE_COMPONENT_TRAIT_DIRECT FALSE

/// When this trait is added, remove the component.
#define TOGGLE_COMPONENT_TRAIT_INVERSE TRUE
